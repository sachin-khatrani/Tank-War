class TankModel {
  int pos;
  String color;
  int fireBomb;
  int shield;
  double angle;
  double power;
  String uid;

  TankModel({this.pos, this.color, this.fireBomb, this.shield, this.angle, this.power, this.uid});
}